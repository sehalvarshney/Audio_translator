# Core API
fastapi
uvicorn[standard]
python-multipart

# Speech-to-Text
faster-whisper

# Translation + Text-to-Speech (Hugging Face)
transformers
accelerate
sentencepiece

# Audio handling
soundfile
numpy

# PyTorch
# NOTE: install this separately with the correct command for your system/CUDA version.
# CPU only:
#   pip install torch
# NVIDIA GPU (CUDA 12.1 example, check https://pytorch.org/get-started/locally for your version):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch
